{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Next.js: Debug Turbopack",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --turbopack",
      "serverReadyAction": {
        "pattern": "- Local:.+(https?://.+)",
        "uriFormat": "%s",
        "action": "debugWithChrome",
        "webRoot": "${workspaceFolder}"
      }
    }
  ]
}
